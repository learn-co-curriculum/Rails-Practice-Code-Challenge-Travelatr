<h1><%= @destination.name %></h1>

<h2>Most recent posts about <%= @destination.name %>:</h2>
  <ul>
  <% @destination.posts.sort_by{|time| time.created_at}.reverse[0..5]. each do |post|%>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
  </ul><br>

  <% blogger_age = 0 %>
  <% @destination.bloggers.uniq.each do |blogger| %>
  <% blogger_age += blogger.age %>
  <% end %>

<h3>Average blogger age: <%= blogger_age * 1.0 / @destination.bloggers.count %></h3>
